story_id: living-docs-overlay
epic_id: delphi-v1-core-loop
methodology: bdd
steps:
  - id: research
    status: complete
    note: Confirmed Doc Scanner (doc-scanner-and-index) and comments table (multica-client-comment-write) as the two implementable sources; idea board integration point has no local spec — genuine open dependency, not a known API.
  - id: implement
    status: partial
    note: >
      SCOPE NOTE: only the backend composer (server/features/living-docs/compose.ts)
      was implemented this pass, covering the two confirmed sources (docs +
      comments) with the idea board explicitly flagged unavailable rather than
      silently omitted. The web/src/features/living-docs/LivingDocView.tsx UI
      component was NOT built this pass — deferred, consistent with this
      story's flex/P1 status ("may slip to v2 if it threatens P0").
  - id: test
    status: complete
    note: 4 tests — cross-source composition, live-refresh (not a snapshot), idea-board explicit unavailable flag, schema-distinctness from Multica's board/task model.
  - id: review
    status: complete
    note: All 63 project tests pass; npm run build succeeds.
  - id: integrate
    status: complete
    note: Committed to feat/delphi-v1-core-loop.
verdict: partial-pass — backend done, UI deferred, both explicitly flagged
finished_at: "2026-07-25T20:54:00Z"
