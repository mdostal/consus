story_id: multica-client-comment-write
epic_id: delphi-v1-core-loop
methodology: tdd
steps:
  - id: research
    status: complete
    note: >
      No live Multica instance available to spike against in this
      environment (flagged risk, carried forward — see commit message and
      architecture.md Risks). Built against Multica's documented self-hosted
      API pattern (server-url, 30s default timeout) from CLI_AND_DAEMON.md.
  - id: test-spec
    status: complete
    note: >
      2 tests in client.test.ts (writes via configured server URL + returns
      remote id; surfaces failure on unreachable) + 2 tests in
      write-comment.test.ts (writes to Multica first then caches locally;
      does NOT write locally on failure).
  - id: implement
    status: complete
    note: >
      server/adapters/multica/client.ts (HttpMulticaClient, injectable
      fetch for testability), server/adapters/multica/write-comment.ts
      (writeCommentAndCache — Multica is the source of truth, local
      comments table is a cache, never written on failure), comments table
      added to migrate.ts.
  - id: review
    status: complete
    note: All 27 project tests pass; npm run build succeeds.
  - id: integrate
    status: complete
    note: Committed to feat/delphi-v1-core-loop.
verdict: pass
finished_at: "2026-07-25T20:47:00Z"
