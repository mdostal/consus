# Consus — what it is

**Consus is the process, not just a screen.** It is the loop we run to turn an idea into approved, built work:

> **ideation → creation → iterate → sign-off / approval**

…including the **CBAs** and the **back-and-forth Q&A** along the way. The `/consus` decision surface only *renders* this process — the process is the thing.

## A Consus cycle
1. **Ideate / frame** — surface the problem or idea (from the idea board, a request, a KPI drop).
2. **Research / CBA** — deep-dive, cost-benefit, ground it in reality (e.g. the 8-repo orchestration deep-dive).
3. **Q&A / iterate** — the back-and-forth that sharpens scope, boundaries, and approach.
4. **Full-spec document** — the deliverable: a solid document with full specs (the plan).
5. **Sign-off** — approval fires the work off; approved CBAs/docs become shared-truth KB.

## This session was a Consus cycle — run by hand
- **Research/CBA:** the orchestration CBA (8 repos, code + PRs + git history) → `orchestration-cba.html`.
- **Q&A/iterate:** the role-tree, event-backbone, locking, and specialist discussion.
- **Full-spec document:** the phased consolidation plan → `orchestrator-plan.html`.

That CBA + plan is the **pending first real Consus package**.

## Bootstrap ordering (important)
The plan → **Consus formal approval** routing **fires once the orchestrator's first slice is solid** — because the orchestrator is what *runs* Consus. Until then, **we are Consus** (manual). We are building the machine that will run this loop; we don't route this plan through a Consus tool that doesn't exist yet.

_Artifacts: CBA → https://claude.ai/code/artifact/403f7c30-b09f-424b-83c1-1f84a8349e24 · Plan → https://claude.ai/code/artifact/00372e22-fb05-4b3a-8d4d-fc2db8f94086_
